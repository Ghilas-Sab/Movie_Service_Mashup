# client

**Exercice 5** — **Propriétaire : Kamlia**

Client en ligne de commande.

À implémenter (voir section 5 du sujet) :
- Appel de `findMovieInformation(title)` sur `VirtualMoviesInformationService` (module `virtual`)
- Appel de `addMovie(MovieDto)` sur le service Thrift (module `thrift`)
- Affichage du résultat de chaque opération
- Utilisation de patterns/DTOs recommandée pour découpler le client des implémentations concrètes

Dépend de : `virtual`, `thrift`
