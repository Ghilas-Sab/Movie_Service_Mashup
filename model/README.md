# model

**Exercice 1** — **Propriétaire : Ghilas**

Modèle métier de la base de films de Walter White.

À implémenter (voir Figure 1 du sujet) :
- `MovieModel` (interface) : `addMovie`, `findMovieByTitle`, `findMoviesByYear`
- `MovieModelImpl` : implémentation, liste `movies` pré-remplie via bloc `static`
- `MovieModelFactory` (singleton) : `getModel()`
- `Movie`, `VisualisationInfo`
- `MovieNotFoundException`
